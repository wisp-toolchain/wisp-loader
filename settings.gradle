pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenLocal()
        maven {
            url = "https://maven.fabricmc.net/"
        }
    }
}

rootProject.name = 'wisp-loader'

include("mixin")
include("minecraft")